services:
  mastra-data-init:
    image: busybox
    user: "0:0"
    command: ["chown", "-R", "${LIBRARY_UID:-1000}:${LIBRARY_GID:-1000}", "/app/data"]
    volumes:
      - ${MASTRA_DATA_SOURCE:-mastra-data}:/app/data
    restart: "no"

  mastra:
    image: ghcr.io/aoton0029/intrahub-mastra:0.1.1
    restart: unless-stopped
    user: "${LIBRARY_UID:-1000}:${LIBRARY_GID:-1000}"
    depends_on:
      mastra-data-init:
        condition: service_completed_successfully
      knowledge-init:
        condition: service_completed_successfully
    environment:
      TZ: ${TZ:-UTC}
      LITELLM_BASE_URL: http://litellm:4000/v1
      MEDIAVAULT_API_BASE_URL: http://mediavault-api:8080
      MEDIAVAULT_MCP_URL: http://mediavault-mcp:8081
      MEDIAVAULT_MCP_TOKEN: ${MCP_AUTH_TOKEN:?MCP_AUTH_TOKEN is required}
      LLM_MODEL: ${MASTRA_LLM_MODEL:-anthropic}
      LITELLM_API_KEY: ${LITELLM_MASTER_KEY:?LITELLM_MASTER_KEY is required}
      KNOWLEDGE_ROOT: /workspace
      AI_WORKSPACE_DIR: /workspace/ai-workspace
      SECOND_BRAIN_DIR: /workspace/second-brain
      TURSO_DATABASE_URL: file:/app/data/mastra.db
      DUCKDB_PATH: /app/data/mastra.duckdb
    ports:
      - "${BIND_ADDRESS:-127.0.0.1}:4111:4111"
    volumes:
      - ${MASTRA_DATA_SOURCE:-mastra-data}:/app/data
      - ${LIBRARY_SOURCE:-shares}:/library
      - ${KNOWLEDGE_SOURCE:-knowledge}:/workspace
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://localhost:4111').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))\""]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    security_opt:
      - no-new-privileges:true
    networks:
      - llm-api
      - mediavault-api

networks:
  llm-api:
  mediavault-api:

volumes:
  mastra-data:
  shares:
  knowledge:
